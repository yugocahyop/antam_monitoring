import 'dart:convert';
// import 'dart:html';
import 'dart:typed_data';

void downloadFile(Uint8List data, String fileName){
  // var data = utf8.encode(url2);
  // var url = Url.createObjectUrlFromBlob(Blob([data]));
  // AnchorElement(href: url)
  //       ..setAttribute('download', fileName)
  //       ..click();
}